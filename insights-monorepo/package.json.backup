{
  "name": "insights-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm --workspace apps/api run dev",
    "build": "npm -w packages/schemas run build && npm -w packages/llm run build && npm -w packages/core run build && npm -w apps/api run build"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
